import colors from '../colors';
import commomStyles from '..';
const dark = {
  pageBackgroundColor: colors.GRAY[900],
  text: colors.GRAY[500],
  primary: colors.GRAY[500],
  accent: colors.YELLOW,
  ...commomStyles
};

export default dark;

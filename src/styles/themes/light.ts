import colors from '../colors';
import commomStyles from '..';
const light = {
  pageBackgroundColor: colors.GRAY[500],
  text: colors.GRAY[900],
  primary: colors.RED[900],
  accent: colors.YELLOW,
  ...commomStyles
};

export default light;
